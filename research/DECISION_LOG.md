# Decision Log

| Date | Decision | Alternatives | Evidence / reason | Reopen when |
|---|---|---|---|---|
| 2026-09-08 | Freeze browser/Playwright adapter work. | Build upstream adapter immediately. | Scientific value not established; generic event mapping already exists. | A validated study shows reusable instrumentation need. |
| 2026-09-08 | Reject screenshot count as perception metric. | Use screenshots/edit ratio. | Native screenshot count behaves mainly like effort/runtime and misses shell-routed capture. | Never as primary construct; may remain negative control. |
| 2026-09-08 | Do not use `evidence_authenticity` or `is_hack` as independent ground truth. | Promote judge scores to primary DV. | Same judge produces coupled fields from same evidence. | Independent calibration/human audit demonstrates validity for a defined use. |
| 2026-09-08 | E5.1 cannot be primary on low GPT-5.4 reruns. | PIL_FAKE_RENDER vs control as headline. | Only 7 occurrences across 4 tasks. | A larger independent run/model yields adequate task-level support. |
| 2026-09-08 | Promote scene provenance / causal lineage to current candidate mechanism. | File provenance only; loop count only. | Genuine capture can still depict agent-created substitute state. | Recoverability audit fails. |
| 2026-09-08 | Do not claim structural provenance itself as novelty. | "Nobody uses dataflow graphs." | AgentTrails, PROV-AGENT, CHIEF already occupy provenance/causal-graph territory. | Never; novelty must be narrower. |
| 2026-09-08 | Next action = provenance recoverability audit on judge-localized spans. | Full event extractor; BPE; application prose; causal rollouts. | Highest-information, lowest-cost test of whether structural scene lineage exists in traces. | Gate resolved. |
| 2026-09-09 | Retrieve trace members by ZIP-over-HTTP range access rather than downloading archives. | Full 9.6 GB download; local mirror. | Fetched 1.79 GB of text members, zero screenshot payloads; every member CRC-verified. | A member set is needed that range access cannot serve. |
| 2026-09-09 | Derive required deliverables and required target applications from WeaveBench task `.md` specs, not from `score.json`. | Use judge `artifact_checks[].task_says` / `id`. | Keeps the target definition independent of the coupled judge, per CLAUDE.md. | Task specs stop covering a needed field. |
| 2026-09-09 | Classify a delivered image's obligation (capture vs synthesis) from the task text before treating any synthesis lineage as a violation. | Treat DIRECT_SYNTHESIS as fabrication. | A diff map or histogram deliverable is legitimately synthesized; without this the instrument falsely attributes. | Never; this is a correctness requirement. |
| 2026-09-09 | Forbid WEAK (temporal-adjacency) evidence from producing a scene classification; record it as `weak_scene_hypothesis` instead. | Let nearest-preceding-launch set the scene class. | Lookback sweep (E020, corrected) shows coverage rises 4.8%→54.9% purely with the parameter while EXACT/STRONG stays ~flat; that is speculation, not evidence. | A trace source supplies real focus/window identity. |
| 2026-09-09 | Require a launched program to be GUI-capable (syntactic check on agent-authored source) before it can be a capture's scene source. | Accept any preceding `python x.py` launch. | Manual validation refuted 3 claims where the "source" was a headless CLI script or a web server (E024). | Never; this was a false-attribution fix. |
| 2026-09-09 | Treat a full-screen capture's scene source as a set: demote STRONG→WEAK when several GUI processes are plausibly mapped. | Attribute to the single nearest launch. | `view_dashboard_and_editor.png` demonstrably shows two applications; full-screen grabs have no per-window attribution. | Window-scoped capture or focus evidence is present. |
| 2026-09-09 | REJECT E011: general structural scene provenance is not recoverable in this corpus. | Continue building the extractor; widen the parser. | R3 EXACT=0, STRONG 6.6%; R4 17.9%; R5 8.4%; shell-routed captures classify at 5.9% vs 98.3% for direct writes. Audit kill criteria 2, 3 and 5 triggered. | A corpus/runtime that records active-window identity at capture time. |
| 2026-09-09 | Do not build the general scene-provenance extractor; carry the boundary forward as the result. | Build it anyway; or substitute a VLM to fill the missing edges. | The missing edges are absent from the trace, not merely unparsed; substituting a VLM would answer a different question and void the structural claim. | Instrumentation change per report Sect. 9.1. |
| 2026-09-09 | Keep the narrow surviving instrument (direct-synthesis-where-capture-required; bracketed substitute renderer) as a candidate, with recall reported alongside precision. | Drop the project entirely; or report only precision. | 23/453 artifacts, 0/172 false attributions on controls — real but narrow, and useless if its 5% recall is not stated. | Recall can be raised without semantic inference. |
| 2026-09-09 | **GENERAL POST-HOC SCENE PROVENANCE ON CURRENT WEAVEBENCH TRACES = NO-GO.** | Continue building the general extractor; relax temporal rules; add semantic/LLM inference to fill edges. | Artifact/file lineage IS recoverable (R1 40/40, R2 453/453), but the scene-source edge is **usually absent from the recording, not merely difficult to parse**: R3 EXACT=0, STRONG 26/394=6.6%, R4 17.9%, R5 8.4%. Surviving bounded capability: (a) direct-write/synthesis provenance (98.3% classified), (b) exact artifact lineage, (c) high-precision scene attribution in the structurally supported minority (23/453=5.1%, 0/172 false attributions on controls). Manual validation is single-reviewer only; no inter-rater statistic exists. | Only if a corpus/runtime records capture-time scene-source identity. Never by weakening temporal rules or substituting an LLM/VLM. |
| 2026-09-09 | Phase A: re-derive every headline figure from the artifacts before freezing. | Trust the report text. | 10/10 reproduced exactly; the lookback sweep did not and was corrected (E029, E020). Freezing an unverified number would have propagated a stale figure into the threat model. | Never; re-derive on every freeze. |
| 2026-09-09 | Next objective = convert the negative result into an observability boundary, threat model, and minimal candidate capture-provenance contract, to be tested adversarially later. | Write up the negative result alone; or start building a recorder immediately. | The deficit is now localized to a specific missing edge; the useful next artifact is a falsifiable systems hypothesis, not another classifier. | If Phase C shows deficits are not attributable to identifiable missing fields. |
| 2026-09-09 | Model information deficits as conjunctive BUNDLES, not independent fields. | Score each field independently. | Independent scoring reported a single field resolving 83.6% of cases, which is an encoding artifact: z-order without a window set is meaningless. Bundles changed the headline from "one field" to "four fields, none useful alone". | Never; conjunctivity is a property of the fields. |
| 2026-09-09 | Report STRICT and LENIENT coverage separately and quote STRICT. | Report the single best number. | LENIENT reaches 72.5% with three cheap fields only by assuming focus identity settles a full-screen grab — an assumption the audit refuted on real data. | If window-scoped capture becomes the norm. |
| 2026-09-09 | Exclude the 98 `no_target_defined` cases from the coverage universe. | Count them as resolvable. | No capture-side field can decide target-vs-substitute when the task never names a target. That deficit is in the task specification. | If task specs are amended to name targets. |
| 2026-09-09 | Include contract Tier F despite it addressing zero historical cases. | Drop it; or claim it as coverage. | Required by the threat model, not by the data — and labelled as such. Claiming it as coverage would be false. | Never. |
| 2026-09-09 | Predeclare that no tier detects attack A9 (genuine target, agent-manipulated state). | Leave it open and discover it later. | Predeclaring stops a post-hoc reinterpretation of the likely failure as a surprise, and makes the kill criterion meaningful. | Never. |
| 2026-09-09 | Predeclare a clean sweep in Phase I as SUSPICIOUS rather than success. | Treat a pass as validation. | Given capabilities C6 and C13, an adversary that fails everywhere more likely indicates a weak adversary than a strong contract. | Never. |
| 2026-09-09 | Research direction = **MODIFY** (not GO, not NO-GO). | Revive the post-hoc construct; or abandon the area. | The original construct stays rejected, but the failure localised to a named addressable edge and the surviving capability is real though narrow. Object changes from "reconstruct post-hoc" to "what capture-time evidence binds under an adversarial generator". | Phase I kill criterion fires fully. |
| 2026-09-09 | Never add AI attribution trailers to commits (user instruction). | Default harness trailer. | User instruction; recorded in `CLAUDE.md`. The `recoverability-audit-v1` commit was amended to remove one. | Never. |
| 2026-09-09 | Preflight 1: split coverage into INFORMATION_DEFICIT_CLOSURE / SCENE_SOURCE_RESOLUTION / TARGET-SUBSTITUTE_ADJUDICATION; withdraw "83.6% of cases resolved by four fields". | Keep the single "resolved" metric. | The contract itself says complete display metadata can leave a composite scene UNKNOWN, so "resolved" was never supported. Recomputation shows the four display fields adjudicate **0.0%**, and that `CAPTURE_TRIGGER_ACTION_ID` alone "closes" 81% of deficits while adjudicating nothing. | Never; the three are distinct properties. |
| 2026-09-09 | Preflight 2: add contract Tier 0 (evidence claim / target context) as a non-attestation prerequisite. | Keep the 98 no-target cases as a mere exclusion. | They demonstrate a prerequisite: provenance cannot adjudicate target-vs-substitute without a declared target. Tier 0 is evaluator/task specification, explicitly NOT trusted telemetry, and undefined target forces UNKNOWN. | Never. |
| 2026-09-09 | Preflight 3: replace the single OS-observed tier with named trust planes (kernel/runtime, compositor/display-server, privileged-application, trusted-recorder, TEE). | Keep T1-T6. | The planes fail independently; kernel integrity does not protect a compositor observation. Collapsing them hid the assumptions the evaluation must test separately. Central principle preserved: truthfully observed state can still be adversarially arranged. | Never. |
| 2026-09-09 | Preflight 4: correct two overstatements — 13 of 14 capabilities observed (C5 WM_CLASS derived, not observed); AFR guarantee restated as event integrity with completeness an explicit non-goal. | Leave both as written. | "Every capability observed" and "AFR assumes honest actions" were both stronger than the sources support. | Never. |
| 2026-09-09 | Add attack A13 (legitimate viewer displaying agent-authored content) and field `DISPLAYED_RESOURCE_LINEAGE`. | Treat A4 (browser) as the only instance. | A13 is the generic form: every process/window fact is genuine, so Tiers A/B/D report authentic. It tests whether the contract follows the provenance of the displayed RESOURCE or stops at the displaying PROCESS. | Never. |
| 2026-09-09 | Rename Phase I to WHITE-BOX ADVERSARIAL CASE EVALUATION. | Call it an adversarial sufficiency experiment. | The adversary is contract-aware but the attacks are constructed and predeclared; it does not evaluate a fully adaptive agent. Adaptive red-teaming is a later phase. | Never. |
| 2026-09-09 | Execute Phase I as a CONTRACT-LOGIC evaluation and declare the empirical arm unrun, rather than skipping the phase or presenting analytic results as measurements. | Wait for a Linux host; or run it and describe it as empirical. | The host has no X11/compositor/recorder. The contract's decision rules are still falsifiable analytically, and that is where the kill question lives. Presenting it as empirical would have been fabrication. | When an instrumented X11 (and Wayland) host exists. |
| 2026-09-09 | Mark S1 (atomicity race) and S2 (nested compositor) BLOCKED rather than modelling a single outcome. | Model one plausible outcome each. | A single modelled timing outcome is not evidence that a race is closed; the spec explicitly requires repeated trials. | When a real recorder exists. |
| 2026-09-09 | Fix two adjudicator defects found mid-run (claim-kind routing; unmappable application identity) and disclose both. | Leave them, or silently correct them. | Both caused FALSE REJECTS and both were fixed using Tier 0 fields declared before the experiment. Neither adds a field nor weakens a rule. Defect 2 exposed a real contract gap, recorded not patched. | Never; disclosure is the point. |
| 2026-09-09 | Add two sensitivity arms removing `displayed_resource_lineage` and the privileged browser channel. | Report only the optimistic configuration. | The contract itself concedes that field is not OS-observable and most apps do not report it, so the arm without it is the DEFAULT deployment, not a degraded one. Removing an optimistic assumption is not adding a field. | Never. |
| 2026-09-09 | Report the kill criterion as NOT FIRED, while stating that the contract evades rather than detects A13-evasive. | Reinterpret abstention as failure to fire the criterion; or claim the contract passed. | The criterion asks whether fabricated evidence can SATISFY the field set. It cannot. It can EVADE it. Both readings are stated; the predeclared criterion is not rewritten. | Never. |
| 2026-09-09 | Name the assurance ceiling as the displayed RESOURCE, not the displaying PROCESS. | Conclude the contract is adequate. | 13b keeps every process/window/capture observable genuine and is undetected at FULL. A contract binding pixels to a process is defeated by pointing a legitimate process at an illegitimate resource. | If a deployable resource-disclosure mechanism is demonstrated. |
| 2026-09-09 | Next step is NOT to add fields: run the two blocked cases and determine whether an evaluator can action UNKNOWN. | Add fields to catch 13b. | The contract's entire safety margin is abstention, which is worthless if UNKNOWN is accepted by default. And Tier D may be invalidated wholesale by S1/S2. | After the empirical arm exists. |
| 2026-09-09 | Tag the analytic arm `phase1-analytic-v1` before touching experimental infrastructure. | Move straight to the empirical build. | The analytic result must remain quotable and unrewritten after empirical results arrive. | Never. |
| 2026-09-09 | Build a real headless X11 lab (Docker + Xvfb + Openbox) rather than defer the empirical arm again. | Wait for a Linux host. | Docker Desktop was startable on this machine; a container gives a reproducible real X server, real windows and real pixels, which is exactly what the analytic arm could not provide. | Never. |
| 2026-09-09 | Resolve PIDs via X-Resource, not `_NET_WM_PID` (IC-1). | Use the conventional `xdotool getwindowpid`. | `_NET_WM_PID` is client-asserted and was absent on every window observed; the contract specifies ACTIVE_PID as kernel/runtime-observed. This corrects an implementation/spec disagreement, not the contract. | Never. |
| 2026-09-09 | Re-specify case 13b so the A13-evasive attack is real (IC-4), and discard the earlier verdict. | Keep the first 13b run. | The first construction disclosed the path in argv and never rendered the fabrication, so its SUBSTITUTE verdict was right for the wrong reason. | Never. |
| 2026-09-09 | **Record the empirical kill-criterion firing and change nothing in the contract.** | Add content-hash comparison to catch 13b. | The criterion was predeclared precisely so the result could not be engineered away. Fabricated evidence satisfied the strongest practical field set from an unprivileged attacker with no boundary compromise. | Never during this experiment; a v2 contract must be predeclared separately. |
| 2026-09-09 | Report the analytic arm as falsified in the unsafe direction rather than merely incomplete. | Present analytic and empirical as complementary. | 5 of 169 pairs moved UNKNOWN -> AUTHENTIC_TARGET, including the headline case. Analyst-derived field vectors were cleaner and more content-aware than a real recorder's. | Never. |
| 2026-09-09 | Surface the UNKNOWN policy result as the largest measured effect. | Report only tier results. | MODE 1 vs MODE 2 moves exposure from 100% to 0% below Tier D and 27.3% to 9.1% at the strongest tier — larger than any field's contribution. | Never. |
| 2026-09-09 | **FREEZE THE KILL DECISION: capture-time system/display provenance is INSUFFICIENT for visual claim integrity under the tested white-box adversary.** Primary falsifier: case 13b. Assurance boundary recorded as `process/surface identity != displayed-resource identity`. | Treat 13b as a missing field and extend the contract. | A legitimate viewer/process/window displayed attacker-authored content while every process and display field remained truthful, at the full ladder, from an unprivileged attacker, with no recorder/kernel/compositor compromise. | Never for the frozen contract. A successor contract must be predeclared separately before testing. |
| 2026-09-09 | Mark the capture-provenance contract branch CLOSED / INSUFFICIENT and forbid adding displayed-resource fields to it. | Patch the contract to catch 13b. | The kill criterion was predeclared precisely so the result could not be engineered away. Binding pixels to a path instead of a process moves the boundary from `process != resource` to `path != content` and no further. | Never. |
| 2026-09-09 | Record the write-API-keyed journal as the primary threat to the 13b mechanism, and keep it attached to the finding. | Report 13b without the caveat; or discard 13b as an instrumentation bug. | Honesty in both directions: an inotify-based journal might have caught this instance, AND the criterion still fired on its own terms. Future work must test it rather than assume either way. | When an event-based journal is built and 13b re-run. |
| 2026-09-09 | Scope S1 and S2 claims narrowly to the tested implementation and substrate. | State that atomic provenance is impossible, or that nested displays defeat provenance generally. | S1 measured one naive recorder; S2 measured one nested X server. Neither licenses a general impossibility claim. | Never; broaden only with new measurements. |
| 2026-09-09 | Scope the `_NET_WM_PID` finding to the tested environment. | Claim it is universally absent on X11. | GTK/Qt commonly set it. The transferable point is that it cannot be relied upon and is application-asserted regardless. | Never. |
| 2026-09-09 | Register H-RB as a documented option only, with its falsification condition, and do not execute it. | Begin resource-binding work immediately. | Phase I must close as a clean negative result before a successor hypothesis is opened, or the failure gets absorbed into the next design. | When Phase I closure is committed and a successor is predeclared. |

| 2026-09-10 | Resolve 13b STATICALLY over the frozen adjudicator before considering any new telemetry. | Build the filesystem-telemetry ladder (J0/J1/J2) and re-run 13b empirically. | The question "would the frozen rules have changed the verdict given the fact?" is a property of the frozen rules, answerable by reading them. Building an eBPF/fanotify recorder to answer it would have spent a phase to learn something already determined by the specification. | Never; this ordering is the cheap direction. |
| 2026-09-10 | Derive the counterfactual Tier C value by calling FROZEN recorder code with an augmented journal, rather than hand-editing the field. | Write `agent_authored_this_episode` into a copied record directly. | Hand-editing tests what the analyst thinks the field means. Calling `Recorder.resource_lineage_for` tests what the frozen implementation means. The distinction is the whole point of the exercise. | Never. |
| 2026-09-10 | **Classify 13b as C. CONTRACT_UNDERSPECIFICATION**, with B' and C' recorded as configuration-bounded secondary findings rather than discarded. | Force it to B (recorder defect) or A (contract falsifier). | The choice between "contract failure" and "implementation defect" presupposes a fixed observation policy the contract never stated. The recorder's binary predicate was the correct reading of a binary specification; there was no vocabulary in which to say "not observed". Forcing a single label would have destroyed the actual finding. | If a successor contract states an observation policy, then its implementations CAN be judged compliant, and the A/B distinction becomes meaningful again. |
| 2026-09-10 | Run the rule-property probe (same injected fact across all 13 scenarios) before accepting that the observation "repairs" 13b. | Report the 13b flip alone. | The flip looked like a clean fix. The probe showed the identical fact turns the honest control into a FALSE REJECT, because the contract cannot distinguish a task-mandated write from an adversarial one. Reporting the flip alone would have implied a repair that does not exist. | Never; this check belongs on any proposed fix. |
| 2026-09-10 | Record the argv-fallback divergence (finding C') WITHOUT modifying the adjudicator to test it. | Patch the adjudicator to abstain and measure the corrected verdicts. | The brief forbids changing the frozen adjudicator, and patching it would have produced a v2 contract by the back door. The divergence is demonstrable statically; the consequence is labelled INFERENCE, not measured. | When a successor contract is predeclared, this is the first rule to fix. |
| 2026-09-10 | Do NOT build the J0->J1->J2 filesystem telemetry ladder. | Build an eBPF/fanotify/inotify recorder as the next phase. | Process/file/runtime provenance is occupied prior art (AgentProvenance). The static trace showed the unresolved question is not whether the write can be observed, but that observing it does not bind rendered bytes and breaks the honest case. E082 then measured that even fd inspection at the capture instant reveals nothing. | Only with a successor contract that declares an observation policy AND separates task-mandated from adversarial writes. |
| 2026-09-10 | Build ONE lab image carrying BOTH display stacks, with three trust domains (compositor uid 1001, attacker uid 1000, recorder root). | Two separate images, or reuse the frozen X11 lab for the X11 arm. | One image means the two arms differ in display architecture and nothing else. Separating the compositor from the attacker is also a stricter trust model than the frozen lab's, which is required if "the compositor is trusted" is to mean anything. | Never. |
| 2026-09-10 | Report the portal ScreenCast stream arm as BLOCKED with its exact backend error, and refuse to infer stream metadata from documentation. | State what the portal specification says streams contain. | Part 10 RESULT D exists precisely for this. The failure is environmental (PipeWire format negotiation, no DRM device), and saying so is not the same as saying the API would or would not expose client identity. | When a portal stack with a working PipeWire path is available. |
| 2026-09-10 | Report **RESULT B** as the primary platform outcome and record RESULT A as REFUTED in its first half. | Report RESULT A, which was the predeclared expectation. | Measured: `AvailableSourceTypes = 1` - the standard API offers no window source at all, so `capture -> surface` is output-scoped on both platforms and zero of twelve bindings changed. The prediction was wrong in a checkable way and is recorded as wrong. | If a compositor whose portal implements WINDOW capture AND reports the selected source's client identity is tested. |
| 2026-09-10 | State that Wayland improves capture-path INTEGRITY and surface->process DIRECTNESS while improving NO binding evidence level. | Conclude either "Wayland solves scene provenance" or "Wayland is no better than X11". | Both are false. Only the compositor can produce a frame (real improvement); the compositor reports client pid as a first-class field (real improvement); and neither changes what can be attributed. Collapsing to either slogan discards a measured distinction. | Never. |
| 2026-09-10 | Record `app_id` as client-asserted, measured on both arms, and explicitly disclaim the accidental `Eog`/`eog` case difference on X11 as a defence. | Present the X11 case difference as a detection signal. | The attacker sets `prgname`; the case difference is GTK's X11 class-name convention. On Wayland the strings are byte-identical. Treating an accident of toolkit convention as a security property is exactly the error this project exists to avoid. | Never. |
| 2026-09-10 | Record AgentProvenance as prior art with citation status UNVERIFIED. | Cite it as established prior art, or omit it. | It was named by the research director, not pinned to a primary source in this repository. `CLAUDE.md` forbids promoting a user report to VERIFIED FACT. It is adopted as the runtime-layer boundary and flagged for citation before any writeup. | When a primary source is pinned. |
| 2026-09-10 | **STOP TECHNICAL EXPANSION AND PACKAGE.** | Open H-RB, application-state attestation, adaptive red-teaming, more compositors, or more OSes. | Both objectives of this run resolved cleanly and in the same direction: 13b is underspecification, and changing the desktop trust architecture moved zero of twelve bindings. Neither result reopens the board; both sharpen the same negative finding. The predeclared default applies. | Only if a specific new result materially changes the board - e.g. a GNOME/KDE portal that reports selected-source client identity. |
| 2026-09-10 | **FREEZE seven findings as the research package** in `research/FROZEN_FINDINGS.md` (F1 inference-policy sensitivity; F2 absence-of-observation read as absence-of-event; F3 truthful facts describing arranged state; F4 Wayland improved isolation but exported no additional binding; F5 process/surface identity != displayed-resource identity; F6 UNKNOWN needs downstream enforcement and cannot repair a false accept; F7 analytic vectors can be cleaner than real observations). | Keep the findings distributed across Phase I and Phase J documents; or keep working and freeze later. | Both Phase J objectives resolved in the same direction and neither reopened the board. Every frozen number was re-verified against its source artifact before freezing. A defensible negative result with a precisely located boundary is the deliverable. | Only if a specific new result materially changes the board. |
| 2026-09-10 | **Do NOT open H-RB.** | Begin resource-binding work now, on the strength of the 13b classification. | The classification showed the failure is an unstated observation policy, not a missing mechanism; and E082 already bounds H-RB's naive form - open file descriptors at the capture instant reveal nothing, because the renderer closes the file before the capture. Opening H-RB now would absorb an unpackaged negative result into the next design, which is exactly the failure mode the Phase I closure discipline exists to prevent. | When the package is complete AND a successor contract is predeclared with an explicit observation policy and inference policy. |
| 2026-09-10 | Record each frozen finding with its evidence class, evidence IDs, scope, and explicit NON-claims, rather than as bare statements. | Freeze the seven one-line claims as written. | Six of the seven are scoped to constructed suites, one corpus, or one compositor; F7 is an inference from a single measured instance. A bare claim list would be quotable beyond its evidence, which is the specific error this project has corrected repeatedly. | Never. |
