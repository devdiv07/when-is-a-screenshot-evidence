# Decision Log

| Date | Decision | Alternatives | Evidence / reason | Reopen when |
|---|---|---|---|---|
| 2026-09-08 | Freeze browser/Playwright adapter work. | Build upstream adapter immediately. | Scientific value not established; generic event mapping already exists. | A validated study shows reusable instrumentation need. |
| 2026-09-08 | Reject screenshot count as perception metric. | Use screenshots/edit ratio. | Native screenshot count behaves mainly like effort/runtime and misses shell-routed capture. | Never as primary construct; may remain negative control. |
| 2026-09-08 | Do not use `evidence_authenticity` or `is_hack` as independent ground truth. | Promote judge scores to primary DV. | Same judge produces coupled fields from same evidence. | Independent calibration/human audit demonstrates validity for a defined use. |
| 2026-09-08 | E5.1 cannot be primary on low GPT-5.4 reruns. | PIL_FAKE_RENDER vs control as headline. | Only 7 occurrences across 4 tasks. | A larger independent run/model yields adequate task-level support. |
| 2026-09-08 | Promote scene provenance / causal lineage to current candidate mechanism. | File provenance only; loop count only. | Genuine capture can still depict agent-created substitute state. | Recoverability audit fails. |
| 2026-09-08 | Do not claim structural provenance itself as novelty. | "Nobody uses dataflow graphs." | AgentTrails, PROV-AGENT, CHIEF already occupy provenance/causal-graph territory. | Never; novelty must be narrower. |
| 2026-09-08 | Next action = provenance recoverability audit on judge-localized spans. | Full event extractor; BPE; application prose; causal rollouts. | Highest-information, lowest-cost test of whether structural scene lineage exists in traces. | Gate resolved. |
| 2026-09-09 | Retrieve trace members by ZIP-over-HTTP range access rather than downloading archives. | Full 9.6 GB download; local mirror. | Fetched 1.79 GB of text members, zero screenshot payloads; every member CRC-verified. | A member set is needed that range access cannot serve. |
| 2026-09-09 | Derive required deliverables and required target applications from WeaveBench task `.md` specs, not from `score.json`. | Use judge `artifact_checks[].task_says` / `id`. | Keeps the target definition independent of the coupled judge, per CLAUDE.md. | Task specs stop covering a needed field. |
| 2026-09-09 | Classify a delivered image's obligation (capture vs synthesis) from the task text before treating any synthesis lineage as a violation. | Treat DIRECT_SYNTHESIS as fabrication. | A diff map or histogram deliverable is legitimately synthesized; without this the instrument falsely attributes. | Never; this is a correctness requirement. |
| 2026-09-09 | Forbid WEAK (temporal-adjacency) evidence from producing a scene classification; record it as `weak_scene_hypothesis` instead. | Let nearest-preceding-launch set the scene class. | Lookback sweep (E020, corrected) shows coverage rises 4.8%→54.9% purely with the parameter while EXACT/STRONG stays ~flat; that is speculation, not evidence. | A trace source supplies real focus/window identity. |
| 2026-09-09 | Require a launched program to be GUI-capable (syntactic check on agent-authored source) before it can be a capture's scene source. | Accept any preceding `python x.py` launch. | Manual validation refuted 3 claims where the "source" was a headless CLI script or a web server (E024). | Never; this was a false-attribution fix. |
| 2026-09-09 | Treat a full-screen capture's scene source as a set: demote STRONG→WEAK when several GUI processes are plausibly mapped. | Attribute to the single nearest launch. | `view_dashboard_and_editor.png` demonstrably shows two applications; full-screen grabs have no per-window attribution. | Window-scoped capture or focus evidence is present. |
| 2026-09-09 | REJECT E011: general structural scene provenance is not recoverable in this corpus. | Continue building the extractor; widen the parser. | R3 EXACT=0, STRONG 6.6%; R4 17.9%; R5 8.4%; shell-routed captures classify at 5.9% vs 98.3% for direct writes. Audit kill criteria 2, 3 and 5 triggered. | A corpus/runtime that records active-window identity at capture time. |
| 2026-09-09 | Do not build the general scene-provenance extractor; carry the boundary forward as the result. | Build it anyway; or substitute a VLM to fill the missing edges. | The missing edges are absent from the trace, not merely unparsed; substituting a VLM would answer a different question and void the structural claim. | Instrumentation change per report Sect. 9.1. |
| 2026-09-09 | Keep the narrow surviving instrument (direct-synthesis-where-capture-required; bracketed substitute renderer) as a candidate, with recall reported alongside precision. | Drop the project entirely; or report only precision. | 23/453 artifacts, 0/172 false attributions on controls — real but narrow, and useless if its 5% recall is not stated. | Recall can be raised without semantic inference. |
| 2026-09-09 | **GENERAL POST-HOC SCENE PROVENANCE ON CURRENT WEAVEBENCH TRACES = NO-GO.** | Continue building the general extractor; relax temporal rules; add semantic/LLM inference to fill edges. | Artifact/file lineage IS recoverable (R1 40/40, R2 453/453), but the scene-source edge is **usually absent from the recording, not merely difficult to parse**: R3 EXACT=0, STRONG 26/394=6.6%, R4 17.9%, R5 8.4%. Surviving bounded capability: (a) direct-write/synthesis provenance (98.3% classified), (b) exact artifact lineage, (c) high-precision scene attribution in the structurally supported minority (23/453=5.1%, 0/172 false attributions on controls). Manual validation is single-reviewer only; no inter-rater statistic exists. | Only if a corpus/runtime records capture-time scene-source identity. Never by weakening temporal rules or substituting an LLM/VLM. |
| 2026-09-09 | Phase A: re-derive every headline figure from the artifacts before freezing. | Trust the report text. | 10/10 reproduced exactly; the lookback sweep did not and was corrected (E029, E020). Freezing an unverified number would have propagated a stale figure into the threat model. | Never; re-derive on every freeze. |
| 2026-09-09 | Next objective = convert the negative result into an observability boundary, threat model, and minimal candidate capture-provenance contract, to be tested adversarially later. | Write up the negative result alone; or start building a recorder immediately. | The deficit is now localized to a specific missing edge; the useful next artifact is a falsifiable systems hypothesis, not another classifier. | If Phase C shows deficits are not attributable to identifiable missing fields. |
| 2026-09-09 | Model information deficits as conjunctive BUNDLES, not independent fields. | Score each field independently. | Independent scoring reported a single field resolving 83.6% of cases, which is an encoding artifact: z-order without a window set is meaningless. Bundles changed the headline from "one field" to "four fields, none useful alone". | Never; conjunctivity is a property of the fields. |
| 2026-09-09 | Report STRICT and LENIENT coverage separately and quote STRICT. | Report the single best number. | LENIENT reaches 72.5% with three cheap fields only by assuming focus identity settles a full-screen grab — an assumption the audit refuted on real data. | If window-scoped capture becomes the norm. |
| 2026-09-09 | Exclude the 98 `no_target_defined` cases from the coverage universe. | Count them as resolvable. | No capture-side field can decide target-vs-substitute when the task never names a target. That deficit is in the task specification. | If task specs are amended to name targets. |
| 2026-09-09 | Include contract Tier F despite it addressing zero historical cases. | Drop it; or claim it as coverage. | Required by the threat model, not by the data — and labelled as such. Claiming it as coverage would be false. | Never. |
| 2026-09-09 | Predeclare that no tier detects attack A9 (genuine target, agent-manipulated state). | Leave it open and discover it later. | Predeclaring stops a post-hoc reinterpretation of the likely failure as a surprise, and makes the kill criterion meaningful. | Never. |
| 2026-09-09 | Predeclare a clean sweep in Phase I as SUSPICIOUS rather than success. | Treat a pass as validation. | Given capabilities C6 and C13, an adversary that fails everywhere more likely indicates a weak adversary than a strong contract. | Never. |
| 2026-09-09 | Research direction = **MODIFY** (not GO, not NO-GO). | Revive the post-hoc construct; or abandon the area. | The original construct stays rejected, but the failure localised to a named addressable edge and the surviving capability is real though narrow. Object changes from "reconstruct post-hoc" to "what capture-time evidence binds under an adversarial generator". | Phase I kill criterion fires fully. |
| 2026-09-09 | Never add AI attribution trailers to commits (user instruction). | Default harness trailer. | User instruction; recorded in `CLAUDE.md`. The `recoverability-audit-v1` commit was amended to remove one. | Never. |

