# Data

## Included

`weavebench_gpt54_low_joined.csv`

Score-level joined reconnaissance for GPT-5.4 low run1/run2.

## Not yet included

Raw WeaveBench `chat.jsonl` files and extracted `score.json` records.

Do not place multi-GB screenshot corpora here by default.

Prefer:
- selective archive member access;
- source manifests;
- content hashes;
- derived compact tables.

If raw data is too large for git, store it outside git and record a reproducible fetch manifest.
