# Scripts

Expected first utilities after raw-schema inspection:

- selective remote ZIP member lister/fetcher;
- source-manifest builder;
- score/quote normalizer;
- exact quote locator;
- provenance recoverability auditor.

Do not implement all of these preemptively. Inspect raw files first and write the minimum utilities required by `specs/RECOVERABILITY_AUDIT.md`.
